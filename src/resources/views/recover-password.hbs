
<h1 class="title-get-password"><strong>Phục hồi mật khẩu</strong></h1>
<h2 class="title-get-verification">Vui lòng nhập mã xác nhận đã được gửi tới email <strong>{{email}}</strong></h2>
<div class="form-wrapper">
        <form action="/reset-password" method="POST">
            <div class="form-forgot-password">
                    <input
                        type="text"
                        class="form-control"
                        id="verification"
                        name="verification"
                        placeholder="Mã xác nhận"
                    />
                </div>
            <input type="hidden" name="ramdomNumber" value="{{randomNumber}}">
            <input type="hidden" name="email" value="{{email}}">
            <button type="submit" class="button btn-primary">Gửi</button>
        </form>
</div>